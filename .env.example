# Copy this file to .env and adjust values for your environment.
# docker-compose.yml reads POSTGRES_PASSWORD from here (falling back to the
# value below if .env is absent, so a fresh clone still runs with no setup).

POSTGRES_PASSWORD=postgres
