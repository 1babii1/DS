import { MapPin } from 'lucide-react'
import { WorkspaceState } from '@/features/workspace-state/ui/workspace-state'
export default function LocationsPage() { return <WorkspaceState description='Location data will provide a clear view of the offices and sites represented in the organization.' icon={MapPin} label='Organization footprint' title='Locations' /> }
